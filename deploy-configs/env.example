# Ollama 配置
OLLAMA_BASE_URL=http://ollama:11434

# 模型配置
# 嵌入模型（用于文本向量化）
EMBEDDING_MODEL=deepseek-r1:1.5b
# 对话模型（用于生成回答）
CHAT_MODEL=deepseek-r1:1.5b

# 数据目录
DATA_DIR=/data

# Nginx 端口配置
HTTP_PORT=80
HTTPS_PORT=443

# SSL 配置（生产环境建议使用 Let's Encrypt）
# USE_SSL=true  # 设置为 true 启用 HTTPS，需要配置证书
USE_SSL=false

# 域名配置（如果使用域名）
# DOMAIN_NAME=your-domain.com

# Docker镜像配置
# 镜像仓库地址（替换为你的实际仓库）
DOCKER_REGISTRY=your-registry.com
DOCKER_NAMESPACE=your-project
DOCKER_IMAGE_NAME=ai-rag-app
DOCKER_IMAGE_TAG=latest

# 完整镜像地址（自动生成）
# DOCKER_IMAGE_FULL=${DOCKER_REGISTRY}/${DOCKER_NAMESPACE}/${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG}